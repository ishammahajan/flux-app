# Backend Environment Variables

# Server port (default: 3000)
PORT=3000

# Database path (default: ./tasks.db)
# For Railway deployment with volumes, set to /data/tasks.db
DATABASE_PATH=./tasks.db

# Node environment
NODE_ENV=development

# =============================================================================
# Magic Mic - Audio-to-Action Pipeline
# =============================================================================

# Deepgram API key for audio transcription (Nova-2)
# Get your key at: https://console.deepgram.com/
DEEPGRAM_API_KEY=your_deepgram_api_key

# OpenRouter API key for LLM processing
# Get your key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key

# LLM model to use via OpenRouter (default: Claude 3.5 Sonnet)
# Options: anthropic/claude-3.5-sonnet, openai/gpt-4o-mini, meta-llama/llama-3.1-70b-instruct
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet
